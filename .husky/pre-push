#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

# Run build (includes TypeScript checking)
echo "  → Running build..."
npm run build || exit 1

# Note: E2E tests (playwright) are run in CI, not pre-push
# To run locally: npm run test

echo "✅ Pre-push checks passed!"
